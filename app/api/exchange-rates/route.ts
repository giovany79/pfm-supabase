import { NextResponse } from 'next/server';
import { createDashboardClient } from '@/lib/supabase/dashboard-client';
export async function GET() { const client = await createDashboardClient(); const { data: auth } = await client.auth.getUser(); if (!auth.user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const { data, error } = await client.from('exchange_rates').select('*').eq('owner_id', auth.user.id).order('effective_date', { ascending: false }); return error ? NextResponse.json({ error: error.message }, { status: 400 }) : NextResponse.json(data); }
export async function POST(request: Request) { const client = await createDashboardClient(); const { data: auth } = await client.auth.getUser(); if (!auth.user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); const body = await request.json(); const { error } = await client.from('exchange_rates').upsert({ owner_id: auth.user.id, currency: body.currency, rate_to_cop: body.rate_to_cop, effective_date: body.effective_date }, { onConflict: 'owner_id,currency,effective_date' }); return error ? NextResponse.json({ error: error.message }, { status: 400 }) : NextResponse.json({ ok: true }); }
